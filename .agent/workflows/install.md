---
description: Pipeline to install or update PokeFetch globally via pipx
---

// turbo-all

1. Ensure we are in the project root.
2. Run the update command:
   ```bash
   pipx install . --force
   ```
